name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:  
      - uses: actions/checkout@v2    
      - name: download libs
        run:
          pip install pytest
      
      - name: Build make
        run:
          make      
      
      - name: Build test
        run: 
          make test
          